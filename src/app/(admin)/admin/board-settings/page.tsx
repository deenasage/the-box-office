// SPEC: board-column-visibility.md
import { BoardSettingsClient } from "@/components/admin/BoardSettingsClient";

export const metadata = { title: "Board Settings — Admin" };

export default function BoardSettingsPage() {
  return <BoardSettingsClient />;
}
